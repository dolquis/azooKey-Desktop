interface:
  display_name: "azooKey コア変換"
  short_description: "変換パイプライン、リライター、辞書の変更を安全に案内します"
  default_prompt: "Use $azookey-core-conversion to review this core/ conversion change and select the required tests."
